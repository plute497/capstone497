import React, { PureComponent } from 'react';

import {
    View,
    Image,
    Text,
    TouchableOpacity
} from 'react-native';

export default class SiteContent extends Component {
    render() {
        return (
            <View></View>
        );
    }
}